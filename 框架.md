# 考勤系统框架（修正版）

更新时间：2026-05-06

本文档以 `6.考勤系统.pptx` 的验收要求为准，并结合当前仓库中的真实代码状态重新整理。目的不是继续堆“理想目录”，而是明确三件事：

1. 当前框架哪些地方是合理的。
2. 当前框架哪些地方不合理，需要立即修正。
3. 现在还缺哪些模块，后续应按什么顺序实现。

---

## 一、PPT 验收要求拆解

根据 PPT，系统最终必须满足以下要求：

### 1. 架构要求

- 必须是 BS（Browser/Server）架构。
- 前后端分层清晰。
- 前端能在浏览器中完成交互。
- 后端 API 规范。
- 数据设计合理，实体关系清晰。

### 2. 基础考勤

- 浏览器调用摄像头采集人脸。
- 支持手动捕捉和自动捕捉。
- 前端显示采集状态。
- 后端执行活体检测和人脸库比对。
- 返回考勤成功/失败、匹配学生信息、考勤时间。
- 支持筛选查询考勤记录。
- 支持 Excel 导出。

### 3. 合照识别

- 上传班级/活动合照。
- 后端检测并批量识别人脸。
- 可处理 10-50 人的多人合照。
- 生成带学号、姓名的活动参与名单。
- 自动统计学生参与活动次数。
- 形成统计数字、表格、柱状图等报表。

### 4. 情绪分析

- 在考勤和合照识别流程中同步做情绪识别。
- 后端记录学号、姓名、识别时间、情绪类型。
- 前端可查看统计结果。

### 5. 系统安全

- 活体检测能抗照片攻击。
- 活体检测能抗视频攻击。
- 教师/学生账号权限区分。
- 具备异常处理：摄像头失败、上传失败、识别超时等。

### 6. 提交物

- 可运行源码。
- 部署说明。
- 课程设计报告。
- 结果分析与测试材料。

---

## 二、当前项目真实状态

### 1. 已经实现的部分

当前仓库中，后端已经完成了以下内容：

- `FastAPI` 基础骨架已搭好。
- 已有统一返回格式、统一异常处理。
- 已有 JWT 登录鉴权。
- 已区分 `teacher` / `student` 角色。
- 已实现学生信息的增删改查。
- 已实现学生 CSV 批量导入。
- 已实现学生注册照上传与人脸特征提取。
- 已实现基于人脸特征库的单张图片识别考勤。
- 已有 JSON 文件型数据存储。
- 已有系统健康检查和模型状态接口。

### 2. 当前已经存在的主要文件

当前后端真实存在的核心文件如下：

```text
backend/
├── app/
│   ├── main.py
│   ├── api/
│   │   ├── auth.py
│   │   ├── students.py
│   │   ├── attendance.py
│   │   └── system.py
│   ├── core/
│   │   ├── config.py
│   │   ├── deps.py
│   │   ├── exceptions.py
│   │   └── security.py
│   ├── services/
│   │   └── face_engine.py
│   ├── storage/
│   │   ├── json_store.py
│   │   └── repositories.py
│   ├── schemas/
│   │   ├── auth.py
│   │   ├── student.py
│   │   ├── attendance.py
│   │   └── common.py
│   └── utils/
│       └── response.py
└── data/
    ├── json/
    ├── faces/
    ├── features/
    └── uploads/
```

### 3. 当前尚未真正实现的部分

以下内容在 PPT 中是硬性要求，但当前代码中还没有完成，或者只是数据文件占位：

- 前端工程整体不存在，当前没有 `frontend/` 目录。
- 浏览器摄像头采集页面不存在。
- 手动/自动捕捉前端交互不存在。
- 采集状态显示不存在。
- 活体检测引擎不存在。
- 抗照片/抗视频攻击链路不存在。
- 合照识别接口与服务不存在。
- 多人脸检测与批量识别不存在。
- 活动参与次数统计不存在。
- 情绪识别引擎不存在。
- 情绪记录与统计接口不存在。
- Excel 导出不存在。
- 报表页面不存在。
- 日志接口不存在。
- 识别超时、模型加载失败等专项异常演示链路不完整。

---

## 三、对“当前框架是否合理”的判断

结论：**当前框架方向基本合理，但文档描述与真实代码状态不一致，且有几个关键设计点需要立即修正。**

### 1. 当前框架中合理的部分

这些设计可以保留：

- 采用 `Vue3 前端 + FastAPI 后端` 的 BS 结构是合理的。
- 对课程作业来说，使用 `JSON + 图片 + 特征文件` 代替数据库是可行的。
- `api / core / services / storage / schemas / utils` 的分层方向是对的。
- 将人脸识别能力单独抽成 `face_engine.py` 是合理的。
- 预先创建 `users.json`、`students.json`、`attendance_records.json` 等文件是合理的。

### 2. 当前框架中不合理的地方

下面这些地方如果不修正，后面会影响验收和继续开发：

#### （1）文档把“规划中的模块”和“已经完成的模块”混在了一起

当前 `框架.md` 原先的问题不是目录太大，而是它把很多未来模块写得像已经落地了一样，例如：

- `group_photo.py`
- `emotion.py`
- `statistics.py`
- `liveness_engine.py`
- `emotion_engine.py`
- 前端多个 `.vue` 页面

这些在当前仓库中并不存在。这样会导致后续判断进度失真，所以必须改成“已实现”和“待实现”分开写。

#### （2）业务逻辑目前过多堆在 API 层

目前学生管理和考勤的核心逻辑主要写在 `api/students.py`、`api/attendance.py` 中。  
这对当前小规模功能还能工作，但后面一旦加入：

- 活体检测
- 情绪识别
- 合照识别
- Excel 导出
- 统计报表

路由文件会迅速膨胀，而且逻辑无法复用。  
因此后续应补上真正的：

- `student_service.py`
- `attendance_service.py`
- `group_photo_service.py`
- `statistics_service.py`

让 API 层只负责参数接收和结果返回。

#### （3）当前 `face_engine` 只适合单人脸，不适合合照识别

现有实现的核心目标是“单张图片只识别一个人”，这对学生注册照和单人考勤是合适的。  
但 PPT 要求合照识别支持 10-50 人，因此后续必须扩展为两套能力：

- 单人脸能力：注册照、单人考勤使用。
- 多人脸能力：合照批量检测和批量匹配使用。

也就是说，`face_engine.py` 的职责不能只停留在“单人脸提特征”。

#### （4）当前数据设计不足以支撑后续评分点

目前虽然已经有多个 JSON 文件，但实际记录字段还不够。  
尤其是 `attendance_records.json` 目前还缺少这些关键字段：

- `capture_mode`：manual / auto
- `liveness_result`
- `liveness_score`
- `emotion`
- `emotion_score`
- `operator_role`

如果这些字段不补，后面很多统计和报告会不好做。

#### （5）教师/学生权限虽然存在，但“学生账号与学生实体”没有绑定好

当前默认有 `student1` 账号，但它的 `student_id` 还是 `null`。  
这意味着“学生账号”和“学生档案”没有真正关联起来。后续如果要做学生端查询自己的考勤/情绪记录，就必须补上这层绑定关系。

#### （6）当前删除学生是物理删除，不利于历史记录保留

课程作业更适合“禁用/停用”而不是彻底删除。  
原因是：

- 历史考勤记录需要保留。
- 历史活动统计需要保留。
- 删除后会破坏数据关联。

因此后续建议把“删除学生”调整为“软删除/禁用学生”，或者至少默认使用 `is_active=false`。

#### （7）BS 架构在当前仓库里还没有真正闭环

PPT 对前端交互占分很高，尤其是：

- 摄像头调用
- 自动捕捉
- 状态展示
- 结果实时渲染
- 记录筛选

但当前项目只有后端，没有前端工程，因此“当前框架已经完整”这个判断是不成立的。  
更准确的说法是：**后端最小闭环已初步完成，BS 整体闭环尚未完成。**

---

## 四、修正后的推荐总体框架

### 1. 技术栈

为兼顾开发速度、演示效果和课程验收，建议采用：

```text
前端：Vue3 + Element Plus + ECharts
后端：FastAPI
存储：JSON + 图片文件 + 人脸特征 .npy 文件
模型：facenet-pytorch（人脸识别）+ 可插拔活体/情绪模型
```

这个方案的好处是：

- 前端页面开发速度快。
- 表格、弹窗、上传、分页、消息提示都容易实现。
- 统计图表能快速完成。
- 课程答辩时界面完整度更高。

### 2. 分层结构

修正后的分层应明确为：

```text
浏览器前端
    ↓
API 路由层（参数校验、权限控制、返回格式）
    ↓
业务服务层（考勤、学生、人脸库、合照、统计）
    ↓
算法能力层（face_engine、liveness_engine、emotion_engine）
    ↓
存储层（repositories / json_store / 文件存储）
    ↓
JSON 文件、图片文件、特征文件
```

### 3. 修正后的项目目录

下面这个目录结构既对齐 PPT，也尽量贴近当前已经存在的代码：

```text
Time_Attendance/
├── backend/
│   ├── app/
│   │   ├── main.py
│   │   ├── api/
│   │   │   ├── auth.py
│   │   │   ├── students.py
│   │   │   ├── attendance.py
│   │   │   ├── group_photo.py
│   │   │   ├── emotions.py
│   │   │   ├── statistics.py
│   │   │   ├── security_tests.py
│   │   │   └── system.py
│   │   ├── core/
│   │   │   ├── config.py
│   │   │   ├── deps.py
│   │   │   ├── exceptions.py
│   │   │   └── security.py
│   │   ├── services/
│   │   │   ├── face_engine.py
│   │   │   ├── liveness_engine.py
│   │   │   ├── emotion_engine.py
│   │   │   ├── student_service.py
│   │   │   ├── attendance_service.py
│   │   │   ├── group_photo_service.py
│   │   │   ├── statistics_service.py
│   │   │   └── export_service.py
│   │   ├── storage/
│   │   │   ├── json_store.py
│   │   │   ├── repositories.py
│   │   │   └── excel_exporter.py
│   │   ├── schemas/
│   │   │   ├── auth.py
│   │   │   ├── student.py
│   │   │   ├── attendance.py
│   │   │   ├── group_photo.py
│   │   │   ├── emotion.py
│   │   │   ├── statistics.py
│   │   │   └── common.py
│   │   └── utils/
│   │       ├── response.py
│   │       ├── logger.py
│   │       └── validators.py
│   ├── data/
│   │   ├── json/
│   │   ├── faces/
│   │   ├── features/
│   │   ├── uploads/
│   │   │   ├── attendance/
│   │   │   ├── group_photos/
│   │   │   └── attack_samples/
│   │   ├── logs/
│   │   └── reports/
│   ├── requirements.txt
│   └── run.py
├── frontend/
│   ├── src/
│   │   ├── api/
│   │   ├── router/
│   │   ├── views/
│   │   ├── components/
│   │   └── stores/
│   └── package.json
└── docs/
```

这里有一个关键原则：

- **已经存在的文件继续沿用。**
- **新增模块只补在缺口位置，不推倒重来。**

---

## 五、修正后的数据设计

为了后面好写代码、好做统计、好写报告，建议按“逻辑表”的方式管理 JSON 文件。

### 1. `users.json`

建议字段：

- `id`
- `username`
- `password_hash`
- `role`
- `student_id`
- `is_active`
- `created_at`

说明：

- 教师账号 `student_id=null`
- 学生账号必须绑定到 `students.id`

### 2. `students.json`

建议字段：

- `id`
- `student_no`
- `name`
- `class_name`
- `is_active`
- `face_image_path`
- `face_feature_path`
- `feature_status`
- `created_at`

说明：

- 默认不做物理删除。
- 通过 `is_active` 管理停用状态。

### 3. `attendance_records.json`

建议字段：

- `id`
- `student_id`
- `student_no`
- `student_name`
- `class_name`
- `image_path`
- `capture_mode`
- `status`
- `match_score`
- `threshold`
- `liveness_result`
- `liveness_score`
- `emotion`
- `emotion_score`
- `created_at`
- `note`

说明：

- `capture_mode` 用于区分手动/自动捕捉。
- `liveness_result` 用于支撑安全模块评分。
- `emotion` 用于支撑情绪统计。

### 4. `activities.json`

建议字段：

- `id`
- `title`
- `activity_type`
- `activity_date`
- `image_path`
- `face_count`
- `matched_count`
- `unmatched_count`
- `created_at`

### 5. `activity_face_results.json`

建议字段：

- `id`
- `activity_id`
- `student_id`
- `student_no`
- `student_name`
- `match_score`
- `emotion`
- `bbox`
- `status`
- `created_at`

### 6. `emotion_records.json`

建议字段：

- `id`
- `source_type`
- `source_id`
- `student_id`
- `student_no`
- `student_name`
- `emotion`
- `emotion_score`
- `created_at`

说明：

- `source_type` 取值建议为 `attendance` 或 `group_photo`。

### 7. `security_test_records.json`

建议字段：

- `id`
- `test_type`
- `sample_path`
- `result`
- `score`
- `created_at`
- `remark`

### 8. `evaluation_records.json`

建议字段：

- `id`
- `activity_id`
- `actual_student_count`
- `correct_match_count`
- `accuracy`
- `created_at`
- `remark`

---

## 六、修正后的模块划分与实现状态

### 模块 A：系统基础骨架

状态：**已基本完成**

当前已具备：

- FastAPI 启动
- 路由注册
- 统一返回结构
- JSON 初始化
- 基本异常处理

### 模块 B：认证与权限

状态：**部分完成**

当前已具备：

- 登录
- 获取当前用户
- JWT 鉴权
- 教师/学生角色判断

还缺：

- 学生账号与学生档案绑定
- 更细粒度的学生端权限控制

### 模块 C：学生与人脸库管理

状态：**部分完成**

当前已具备：

- 学生增删改查
- CSV 批量导入
- 单人注册照上传
- 特征提取与保存

还缺：

- 更稳妥的停用逻辑
- 学生端关联查询
- 更完整的数据校验

### 模块 D：基础考勤

状态：**部分完成**

当前已具备：

- 上传单张图片进行识别
- 与学生特征库比对
- 记录考勤结果
- 查询考勤记录

还缺：

- 活体检测
- 手动/自动捕捉前端
- 采集状态展示
- 日期/情绪等筛选
- Excel 导出

### 模块 E：活体检测与安全测试

状态：**未实现**

必须补齐：

- `liveness_engine.py`
- 抗照片攻击
- 抗视频攻击
- 安全测试记录接口
- 安全测试页面

### 模块 F：合照识别与活动统计

状态：**未实现**

必须补齐：

- 多人脸检测
- 批量识别
- 活动记录
- 活动参与名单
- 参与次数统计
- 准确率评估

### 模块 G：情绪分析

状态：**未实现**

必须补齐：

- `emotion_engine.py`
- 考勤流程情绪识别
- 合照流程情绪识别
- 情绪记录接口
- 情绪统计页面

### 模块 H：统计报表

状态：**未实现**

必须补齐：

- 数字统计接口
- 表格接口
- 柱状图数据接口
- Excel 导出
- Dashboard / Report 页面

### 模块 I：异常处理与日志

状态：**部分完成**

当前已具备：

- 基础异常捕获

还缺：

- 摄像头失败提示链路
- 上传失败提示链路
- 模型加载失败可视化
- 识别超时处理
- 日志记录与日志查看接口

### 模块 J：文档、部署与实验材料

状态：**未实现**

必须补齐：

- `README`
- 运行说明
- 测试说明
- 报告配套截图和数据

---

## 七、后续还需要实现哪些模块

这是接下来真正要做的开发清单。

### 第一优先级：先补齐能直接拿分的主链路

1. 新建前端工程，完成登录页和考勤页。
2. 实现浏览器摄像头调用。
3. 实现手动拍照和自动捕捉。
4. 实现采集状态显示。
5. 将当前考勤识别逻辑从 `api/attendance.py` 下沉到 `attendance_service.py`。
6. 接入活体检测并写入考勤记录。
7. 为考勤记录补日期、状态、情绪等筛选条件。
8. 实现 Excel 导出。

这部分完成后，基础考勤的大部分分数才真正稳。

### 第二优先级：补齐合照识别主链路

1. 扩展 `face_engine.py`，支持多人脸检测与批量特征提取。
2. 新建 `group_photo_service.py` 和 `group_photo.py`。
3. 实现合照上传、批量识别、活动名单生成。
4. 写入 `activities.json` 与 `activity_face_results.json`。
5. 自动统计每个学生参与活动次数。
6. 增加活动识别准确率评估接口。

这部分直接对应 PPT 中“合照识别”和“活动参与统计”的分值。

### 第三优先级：补齐情绪分析

1. 新建 `emotion_engine.py`。
2. 在考勤识别流程中写入情绪结果。
3. 在合照识别流程中写入情绪结果。
4. 完成 `emotion_records.json` 的记录逻辑。
5. 提供按学生、按来源、按时间统计的接口。
6. 前端做情绪统计图表。

### 第四优先级：补齐安全测试、报表与文档

1. 实现安全测试接口和页面。
2. 实现数字卡片、表格、柱状图报表。
3. 完善系统异常处理与日志。
4. 补部署说明、测试说明、报告支撑材料。

---

## 八、推荐实施顺序

为了避免返工，建议后续严格按下面顺序推进：

```text
1. 前端基础工程（登录、路由、请求封装）
2. 考勤前端页面（摄像头、手动拍照、自动捕捉、状态显示）
3. attendance_service 重构
4. 活体检测模块
5. 考勤记录筛选与 Excel 导出
6. 合照识别模块
7. 活动参与统计与准确率评估
8. 情绪识别模块
9. 统计报表模块
10. 安全测试模块
11. 日志与异常处理完善
12. README、部署和实验报告支撑
```

这个顺序的原因是：

- 先把“基础考勤”做成完整闭环。
- 再扩展“合照识别”和“情绪分析”。
- 最后整理报表、安全测试和文档。

---

## 九、最终结论

### 1. 当前框架是否合理

结论：**方向合理，但当前文档版本不够严谨，且不能再把“计划中的模块”当成“已经存在的结构”。**

### 2. 必须立即修正的地方

- 把框架文档改成“现状 + 修正版方案 + 缺口清单”。
- 后续新增功能必须进入 `service` 层，而不是继续堆在 `api` 层。
- 人脸引擎要扩展为同时支持单人脸和多人脸。
- 数据结构要为活体、情绪、活动统计预留字段。
- 学生账号和学生实体必须绑定。
- 后续默认采用“停用优先”，减少物理删除。

### 3. 当前最缺的模块

按重要性排序，当前最缺的是：

1. 前端工程与摄像头考勤页面
2. 活体检测
3. 自动捕捉与状态显示
4. 考勤筛选与 Excel 导出
5. 合照识别
6. 活动统计
7. 情绪识别
8. 报表页面
9. 安全测试页面与记录
10. 文档与部署材料

---

这个修正版框架以后可以直接作为后续实现的总蓝图使用。
